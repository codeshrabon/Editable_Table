import 'package:flutter/material.dart';

class EditableTableCompund extends StatelessWidget{
  const EditableTableCompund({super.key});
  @override
  Widget build(Object context) {
    // TODO: implement build
    return ();
  }
  
}